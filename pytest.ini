[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests that need fixture data
    performance: marks performance benchmarks
addopts = -v --tb=short --strict-markers
